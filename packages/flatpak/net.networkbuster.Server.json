#!/bin/bash
# Flatpak build manifest for NetworkBuster

{
  "app-id": "net.NETWORKBUSTERSETUP.Server",
  "runtime": "org.freedesktop.Platform",
  "runtime-version": "23.08",
  "sdk": "org.freedesktop.Sdk",
  "sdk-extensions": [
    "org.freedesktop.Sdk.Extension.node18"
  ],
  "command": "NETWORKBUSTERSETUP",
  "finish-args": [
    "--socket=network",
    "--socket=network-service",
    "--share=network",
    "--device=dri",
    "--filesystem=home",
    "--filesystem=/tmp",
    "--env=PATH=/app/bin:/usr/bin",
    "--talk-name=org.freedesktop.Notifications"
  ],
  "modules": [
    {
      "name": "NETWORKBUSTERSETUP",
      "buildsystem": "simple",
      "build-commands": [
        "npm install --production",
        "mkdir -p /app/bin",
        "cp server.js /app/bin/NETWORKBUSTERSETUP",
        "cp -r . /app/lib/NETWORKBUSTERSETUP"
      ],
      "sources": [
        {
          "type": "git",
          "url": "https://github.com/NETWORKBUSTERSETUP/networkbuster.net.git",
          "tag": "v1.0.1"
        }
      ]
    },
    {
      "name": "NETWORKBUSTERSETUP-wrapper",
      "buildsystem": "simple",
      "build-commands": [
        "mkdir -p /app/bin",
        "cat > /app/bin/NETWORKBUSTERSETUP-wrapper << 'EOF'\n#!/bin/bash\ncd /app/lib/NETWORKBUSTERSETUP\nexec node server.js \"$@\"\nEOF",
        "chmod +x /app/bin/NETWORKBUSTERSETUP-wrapper"
      ],
      "sources": []
    }
  ]
}
